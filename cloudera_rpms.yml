---
- hosts: all

  vars_files:
  - var.yml

  tasks:

  - name: Install cloudera rpms
    action: yum pkg={{item}} state=present
    with_items:
    - mysql
    - epel-release
    - mlocate
    - wget

